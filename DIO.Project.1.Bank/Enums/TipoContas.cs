namespace DIO
{
//classe publica enum de nome "TipoConta" a mesma e referenciada no 'Programs.cs'
    public enum TipoConta
    {
//Dados ou valores da classe enum nao mutaveis, so referenciais.
        PessoaFisica = 1,
        PessoaJuridica =2
    }

}